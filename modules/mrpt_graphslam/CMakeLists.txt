#---------------------------------------------
# Macro declared in "DeclareMRPTLib.cmake":
#---------------------------------------------
define_mrpt_lib(
	# Lib name
	graphslam
	# Dependencies
	mrpt-slam
	)

if(BUILD_mrpt-graphslam)

endif()
