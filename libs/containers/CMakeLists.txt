#---------------------------------------------
# Macro declared in "DeclareMRPTLib.cmake":
#---------------------------------------------
define_mrpt_lib_header_only(
	# Lib name
	containers
	# Dependencies
	#mrpt-core
	)

IF(BUILD_mrpt-containers)

ENDIF( )
