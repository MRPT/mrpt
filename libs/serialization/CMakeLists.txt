#---------------------------------------------
# Macro declared in "DeclareMRPTLib.cmake":
#---------------------------------------------
define_mrpt_lib(
	# Lib name
	serialization
	# Dependencies
	mrpt-typemeta
	mrpt-rtti
	)

IF(BUILD_mrpt-serialization)

ENDIF( )
