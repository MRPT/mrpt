#---------------------------------------------
# Macro declared in "DeclareMRPTLib.cmake":
#---------------------------------------------
define_mrpt_lib(
	# Lib name
	serialization
	# Dependencies
	mrpt-typemeta
	)

IF(BUILD_mrpt-serialization)

ENDIF( )
